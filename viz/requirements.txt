# Dataraft Telemetry Dashboard Requirements

# Core framework (compatible versions)
# Note: Dash 2.14.0 requires Werkzeug<2.3.0, so use Flask 2.2.x
flask==2.2.5
dash==2.14.0
dash-bootstrap-components==1.5.0
werkzeug==2.2.3

# Visualization
plotly==5.18.0

# Data processing
pandas>=2.2.0
networkx==3.2.0

# Utilities (should already be in parent requirements)
numpy>=1.24.0
